# Slice P1.1b: SpecContext Core Implementation

**Goal**: Create immutable SpecContext dataclass based on validated dependency analysis

**Slice Type**: Infrastructure

**Helper Dependencies & Search Evidence:**

Helper search performed and validated:
```bash
# Validate dataclass patterns in codebase
rg "@dataclass" /Users/spensermcconnell/__Active_Code/spec-cli --type py
# Expected: Existing dataclass usage patterns for consistency

# Check frozen dataclass usage
rg "frozen=True" /Users/spensermcconnell/__Active_Code/spec-cli --type py
# Expected: Existing frozen patterns or confirmation none exist

# Validate typing imports
rg "from typing import" /Users/spensermcconnell/__Active_Code/spec-cli --type py | head -3
# Expected: Current typing patterns for consistency
```

- **Existing helper**: P1.1a dependency analysis → Validated dependency requirements
- **Existing helper**: spec_cli/utils/error_utils.py → Error handling utilities
- **Existing helper**: spec_cli/logging/timing.py → Dataclass patterns from existing code
- **New helper to create**: spec_cli/utils/context_utils.py → `validate_context_immutability(context: Any) -> bool` and `create_context_hash(context: Any) -> str`

**Complexity Analysis:**
- Decision points: 3/7 (dataclass creation, validation, error handling)
- Helper calls: 2 (context_utils for validation, error_utils for exceptions)
- McCabe validation: Pass - well under 7 complexity with helper usage

**Inputs → Action → Outputs:**
- **Inputs**: {dependency_requirements: DependencyReport, context_config: Dict[str, Any]}
- **Action**:
  1. Create frozen dataclass using requirements from P1.1a analysis
  2. Implement type-safe dependency attributes based on actual requirements
  3. Add validation using context_utils helper for immutability verification
  4. Handle construction errors using error_utils helper
- **Outputs**: {spec_context_class: Type[SpecContext]} or {implementation_error: SpecContextError}

**Files to Create/Modify:**
- `spec_cli/core/context.py` (new - SpecContext dataclass implementation)
- `spec_cli/utils/context_utils.py` (new - context validation helpers)

**Integration Validation:**
SpecContext implementation matches actual dependency requirements from P1.1a analysis, provides immutable dependency aggregation

**Test Requirements:**

- **Unit Tests**:
  - `test_spec_context_creation_when_valid_dependencies_then_creates_immutable_context()` - Test SpecContext creation with valid dependency inputs
  - `test_spec_context_immutability_when_attribute_modification_then_raises_frozen_error()` - Test frozen dataclass prevents attribute modification
  - `test_spec_context_type_safety_when_invalid_dependency_type_then_raises_type_error()` - Test type validation for dependency attributes
  - `test_context_validation_when_context_created_then_validates_immutability()` - Test context_utils helper validates immutability correctly
  - `test_context_hash_when_same_dependencies_then_generates_consistent_hash()` - Test context hash generation for equality comparison
  - `test_spec_context_error_when_invalid_construction_then_raises_specific_error()` - Test error handling for invalid context construction

- **Integration Test**:
  - `test_spec_context_integration_when_p1_1a_requirements_then_implements_complete_context()` - End-to-end test using actual dependency requirements from P1.1a analysis

- **Manual Test**: Create `manual_test_slice_p1_1b_spec_context_implementation.sh` script that:
  - Creates SpecContext instances with various dependency combinations
  - Validates immutability by attempting attribute modifications
  - Tests context validation helper functions
  - Verifies type safety with invalid dependency types
  - Tests context hash generation and equality
  - Shows successful context creation with real dependency objects
  - Returns exit code 0 for success, 1 for failure with detailed output

- **Cross-Slice Integration Testing**:
  - `test_p1_1b_to_p1_2a_context_factory_compatibility()` - Validates SpecContext is compatible with factory interface requirements
  - `test_spec_context_meets_dependency_requirements()` - Validates SpecContext implementation satisfies all requirements identified in P1.1a

- **DI Migration Context Testing**:
  - `test_spec_context_immutability_for_concurrent_access()` - Validates SpecContext thread safety for concurrent CLI operations
  - `test_spec_context_dependency_injection_compatibility()` - Validates SpecContext structure supports dependency injection patterns

**Quality Gate Validation:**
- [ ] Unit test coverage ≥ 90% for SpecContext implementation and validation utilities
- [ ] Integration test validates SpecContext meets P1.1a dependency requirements
- [ ] Manual test script demonstrates SpecContext creation and immutability validation
- [ ] Cross-slice integration confirms SpecContext works with factory patterns
- [ ] Type annotations complete with strict mypy validation for all context attributes
- [ ] Frozen dataclass immutability verified through comprehensive testing
- [ ] Poetry-based quality gates pass (mypy --strict, ruff, bandit, pytest)

**AI Agent Execution Notes:**
- Use dependency requirements from P1.1a to define exact SpecContext attributes
- Follow existing dataclass patterns found in codebase for consistency
- Ensure frozen=True immutability with comprehensive type hints
- Focus on clean, simple implementation that aggregates validated dependencies
