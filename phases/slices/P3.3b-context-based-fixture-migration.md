# Slice P3.3b: Context-Based Fixture Migration

**Goal**: Migrate test fixtures to use context-based patterns based on analysis requirements

**Slice Type**: Component

**Helper Dependencies & Search Evidence:**

Helper search performed and validated:
```bash
# Validate pytest fixture modification patterns
rg "@pytest\.fixture.*scope|yield|return" /Users/spensermcconnell/__Active_Code/spec-cli/tests --type py
# Expected: Current fixture implementation patterns for migration consistency

# Check mock and context creation patterns
rg "Mock\(|MagicMock\(|patch\(" /Users/spensermcconnell/__Active_Code/spec-cli/tests --type py | head -3
# Expected: Current mocking patterns for context factory integration

# Validate test helper usage patterns
find /Users/spensermcconnell/__Active_Code/spec-cli -path "*/test*" -name "*.py" -exec grep -l "helper" {} \;
# Expected: Test files using helper patterns for fixture integration
```

- **Existing helper**: P3.3a test fixture analysis → Fixture migration requirements and dependency patterns
- **Existing helper**: P1.2b factory implementation → SpecContext testing factory methods
- **Existing helper**: spec_cli/utils/error_utils.py → Error handling for migration failures
- **New helper to create**: spec_cli/utils/test_migration_utils.py → `create_context_fixture(factory_method: Callable) -> Callable` and `validate_test_isolation(test_func: Callable) -> bool`

**Complexity Analysis:**
- Decision points: 5/7 (fixture creation, factory integration, isolation validation, migration verification, error handling)
- Helper calls: 2 (test_migration_utils for fixture creation, error_utils for exceptions)
- McCabe validation: Pass - under 7 complexity with helper delegation

**Inputs → Action → Outputs:**
- **Inputs**: {fixture_requirements: TestFixtureReport, context_factory: Callable}
- **Action**:
  1. Create context-based test fixtures using requirements from P3.3a analysis
  2. Integrate SpecContext testing factory from P1.2b for context creation
  3. Replace singleton-dependent fixtures with context fixtures using test_migration_utils helper
  4. Validate test isolation and eliminate state contamination using test_migration_utils helper
- **Outputs**: {migrated_fixtures: TestFixtureMigration} or {migration_error: TestMigrationError}

**Files to Create/Modify:**
- `tests/conftest.py` (modify - add context-based fixtures, remove singleton fixtures)
- `spec_cli/utils/test_migration_utils.py` (new - test fixture migration helpers)

**Integration Validation:**
Migrated test fixtures provide isolated contexts for testing, eliminate state contamination, and improve test reliability

**Test Specifications:**

**Unit Tests:** (6+ tests in `tests/unit/utils/test_test_migration_utils.py`)
- `test_create_context_fixture_when_factory_method_provided_then_creates_valid_fixture`
- `test_create_context_fixture_when_invalid_factory_then_raises_migration_error`
- `test_validate_test_isolation_when_isolated_test_then_returns_true`
- `test_validate_test_isolation_when_state_contamination_then_returns_false`
- `test_context_fixture_migration_when_singleton_fixtures_replaced_then_no_shared_state`
- `test_test_migration_when_context_integration_then_uses_spec_context_factory`
- `test_fixture_migration_when_isolation_validation_then_prevents_state_contamination`

**Integration Test:** (1 test in `tests/integration/test_context_based_fixture_migration.py`)
- `test_context_based_fixture_migration_when_full_test_suite_then_isolated_reliable_testing`

**Manual Test:** (Shell script with validation)
```bash
#!/bin/bash
# Test context-based fixture migration
poetry run pytest tests/unit/ -v --tb=short
UNIT_TESTS=$?
poetry run pytest tests/integration/ -v --tb=short
INTEGRATION_TESTS=$?

if [ $UNIT_TESTS -eq 0 ] && [ $INTEGRATION_TESTS -eq 0 ]; then
  echo "PASS: Context-based fixture migration maintains test functionality"
  exit 0
else
  echo "FAIL: Context-based fixture migration broke tests"
  exit 1
fi
```

**Cross-Slice Integration Testing:**
- `test_fixture_migration_when_analysis_requirements_implemented_then_meets_migration_specs`
- `test_context_fixtures_when_used_with_migrated_commands_then_provides_consistent_context`

**DI Migration Context Testing:**
- `test_context_fixtures_when_spec_context_factory_used_then_creates_isolated_test_contexts`
- `test_fixture_migration_when_singleton_elimination_complete_then_no_shared_state_between_tests`

**Quality Gate Validation:**
- [ ] All singleton-dependent fixtures replaced with context-based alternatives
- [ ] Test isolation verified with no state contamination between test executions
- [ ] Context fixtures integrate with SpecContext testing factory from P1.2b
- [ ] Migrated fixtures follow requirements from P3.3a analysis
- [ ] Test reliability improved with isolated context creation
- [ ] Fixture migration maintains all existing test functionality
- [ ] Context fixture patterns are reusable for future test development

**AI Agent Execution Notes:**
- Use fixture migration requirements from P3.3a to guide specific fixture changes
- Integrate SpecContext testing factory from P1.2b for consistent context creation
- Replace singleton-dependent test fixtures with context-based alternatives
- Focus on achieving complete test isolation with no shared state between test executions
