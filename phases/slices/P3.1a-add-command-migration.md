# Slice P3.1a: Add Command Migration

**Goal**: Migrate add command to use context injection decorator

**Slice Type**: Component

**Helper Dependencies & Search Evidence:**

Helper search performed and validated:
```bash
# Validate add command current implementation
find /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli -name "*add*" -name "*.py"
# Expected: add.py command file location and structure

# Check add command current signature and dependencies
rg "def.*add|@click\.command.*add" /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli --type py
# Expected: Current add command implementation details

# Identify singleton usage in add command
rg "settings|console|progress|singleton" /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli/commands/add.py
# Expected: Current dependency access patterns in add command
```

- **Existing helper**: P2.2b context injection decorator → Context injection decorator implementation
- **Existing helper**: P2.3b migration utilities → Command migration validation from previous command migrations
- **Existing helper**: spec_cli/utils/error_utils.py → Error handling for migration issues

**Complexity Analysis:**
- Decision points: 4/7 (decorator application, signature migration, dependency replacement, behavior validation)
- Helper calls: 2 (migration_utils for signature changes, error_utils for exceptions)
- McCabe validation: Pass - under 7 complexity with helper delegation

**Inputs → Action → Outputs:**
- **Inputs**: {add_command_file: Path, existing_migration_patterns: Dict[str, Any]}
- **Action**:
  1. Apply context injection decorator to add command using P2.2b implementation
  2. Update command signature to accept context parameter using migration_utils helper
  3. Replace singleton access with context attribute access
  4. Validate migrated command maintains identical behavior using migration_utils helper
- **Outputs**: {migrated_add_command: Callable} or {migration_error: MigrationError}

**Files to Create/Modify:**
- `spec_cli/cli/commands/add.py` (modify - apply decorator and use context)

**Integration Validation:**
Migrated add command works identically to previous behavior while using context injection consistently with init/status commands

**Test Specifications:**

**Unit Tests:** (6+ tests in `tests/unit/cli/commands/test_add_migration.py`)
- `test_add_command_when_context_decorator_applied_then_receives_context_parameter`
- `test_add_command_when_using_context_settings_then_accesses_settings_from_context`
- `test_add_command_when_using_context_console_then_accesses_console_from_context`
- `test_add_command_when_singleton_replaced_then_no_singleton_references_remain`
- `test_add_command_when_migrated_signature_then_maintains_click_compatibility`
- `test_add_command_when_behavior_validation_then_identical_to_original_behavior`
- `test_add_command_when_context_injection_error_then_raises_migration_error`

**Integration Test:** (1 test in `tests/integration/test_add_command_migration.py`)
- `test_add_command_migration_when_full_workflow_then_consistent_with_other_migrated_commands`

**Manual Test:** (Shell script with validation)
```bash
#!/bin/bash
# Test migrated add command behavior
cd test_area
spec init
echo "test content" > test.txt
spec add .specs/test.md
if [ $? -eq 0 ]; then
  spec status | grep -q "new file"
  if [ $? -eq 0 ]; then
    echo "PASS: Add command migration maintains functionality"
    exit 0
  else
    echo "FAIL: Add command behavior changed"
    exit 1
  fi
else
  echo "FAIL: Add command migration failed"
  exit 1
fi
```

**Cross-Slice Integration Testing:**
- `test_add_command_when_used_with_migrated_init_then_context_consistent`
- `test_add_command_when_used_with_migrated_status_then_dependency_injection_works`

**DI Migration Context Testing:**
- `test_add_command_when_context_factory_provides_dependencies_then_uses_injected_instances`
- `test_add_command_when_singleton_elimination_complete_then_no_global_state_access`

**Quality Gate Validation:**
- [ ] Add command uses context injection decorator from P2.2b
- [ ] All singleton references replaced with context attribute access
- [ ] Command behavior identical to pre-migration implementation
- [ ] Migration follows patterns from P2.3b and P2.3c
- [ ] No remaining singleton import or usage patterns
- [ ] Context parameter properly typed and validated
- [ ] Error handling maintains existing patterns while using context

**AI Agent Execution Notes:**
- Follow exact migration patterns established in P2.3b and P2.3c for consistency
- Apply context injection decorator from P2.2b to add command function
- Replace singleton access patterns with context.settings, context.console, etc.
- Ensure add command behavior is preserved exactly while changing internal implementation
