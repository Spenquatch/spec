# Slice P2.2b: Context Injection Decorator Implementation

**Goal**: Implement decorator for automatic context injection using validated patterns

**Slice Type**: Integration

**Helper Dependencies & Search Evidence:**

Helper search performed and validated:
```bash
# Validate Click command function patterns
rg "@click\.command" /Users/spensermcconnell/__Active_Code/spec-cli --type py | head -3
# Expected: Click command decorator patterns for compatibility

# Check existing function wrapper patterns
rg "def.*wrapper|functools\.wraps" /Users/spensermcconnell/__Active_Code/spec-cli --type py
# Expected: Function wrapping patterns for consistency

# Validate parameter inspection capabilities
rg "func\.__.*__|getattr\(func" /Users/spensermcconnell/__Active_Code/spec-cli --type py
# Expected: Function metadata access patterns
```

- **Existing helper**: P2.2a decorator analysis → Decorator design requirements and signature validation
- **Existing helper**: P2.1b Click integration → Context storage and retrieval utilities
- **Existing helper**: spec_cli/utils/error_utils.py → Error handling utilities
- **New helper to create**: spec_cli/utils/decorator_utils.py → `create_context_injector(context_retriever: Callable) -> Callable` and `preserve_function_metadata(wrapper: Callable, original: Callable) -> Callable`

**Complexity Analysis:**
- Decision points: 4/7 (decorator creation, parameter injection, metadata preservation, error handling)
- Helper calls: 3 (decorator_utils for injection logic, click_utils for context retrieval, error_utils for exceptions)
- McCabe validation: Pass - under 7 complexity with helper delegation

**Inputs → Action → Outputs:**
- **Inputs**: {command_function: Callable, context_retrieval_strategy: Callable}
- **Action**:
  1. Create context injection decorator using design from P2.2a analysis
  2. Implement parameter injection using Click context utilities from P2.1b
  3. Preserve function metadata using decorator_utils helper
  4. Handle decorator application errors using error_utils helper
- **Outputs**: {decorated_command: Callable} or {decorator_error: DecoratorError}

**Files to Create/Modify:**
- `spec_cli/cli/decorators.py` (new - context injection decorator implementation)
- `spec_cli/utils/decorator_utils.py` (new - decorator implementation helpers)

**Integration Validation:**
Context injection decorator automatically provides context to CLI commands using validated Click integration patterns

**Test Specifications:**

**Unit Tests (6+ functions):**
```python
# tests/unit/cli/test_decorators.py
class TestContextInjectionDecorator:
    def test_context_decorator_when_valid_function_then_injects_context_parameter(self)
    def test_context_decorator_when_missing_context_then_raises_decorator_error(self)
    def test_context_decorator_when_preserves_metadata_then_maintains_function_attributes(self)
    def test_context_decorator_when_click_command_then_maintains_click_compatibility(self)
    def test_context_decorator_when_multiple_decorators_then_preserves_decorator_chain(self)
    def test_context_decorator_when_invalid_signature_then_raises_signature_error(self)
    def test_context_decorator_when_context_retrieval_fails_then_raises_context_error(self)

    @pytest.fixture
    def mock_context(self):
        return Mock(spec=SpecContext)

    @pytest.fixture
    def mock_click_context(self):
        return Mock(spec=click.Context)

    @pytest.fixture
    def sample_command_function(self):
        def sample_command():
            return "test_result"
        return sample_command

# tests/unit/utils/test_decorator_utils.py
class TestDecoratorUtils:
    def test_create_context_injector_when_valid_retriever_then_returns_decorator(self)
    def test_preserve_function_metadata_when_valid_functions_then_copies_attributes(self)
    def test_create_context_injector_when_invalid_retriever_then_raises_type_error(self)
```

**Integration Test (1 end-to-end):**
```python
# tests/integration/cli/test_context_injection_integration.py
class TestContextInjectionIntegration:
    def test_decorated_command_execution_with_full_context_chain(self, tmp_path):
        """Test complete context injection flow from CLI entry to command execution."""
        # Setup: Create test CLI app with context injection
        # Action: Execute decorated command with context
        # Assert: Context properly injected and command executes successfully
```

**Manual Test (shell script):**
```bash
# Test context injection decorator functionality
cd test_area
spec init

# Test 1: Verify decorator preserves Click functionality
python -c "
from spec_cli.cli.decorators import context_injection
from spec_cli.cli.commands.init import init_command
import inspect
assert hasattr(init_command, '__click_params__')
print('✓ Click compatibility preserved')
"

# Test 2: Verify metadata preservation
python -c "
from spec_cli.cli.decorators import context_injection
@context_injection
def test_func():
    '''Test docstring'''
    pass
assert test_func.__doc__ == 'Test docstring'
print('✓ Function metadata preserved')
"

# Test 3: Verify decorator chain compatibility
python -c "
import click
from spec_cli.cli.decorators import context_injection
@click.command()
@context_injection
def test_command():
    return 'success'
assert callable(test_command)
print('✓ Decorator chain compatibility confirmed')
"
```

**Cross-Slice Integration Testing (2+ tests):**
```python
# Test P2.2a → P2.2b: Decorator design to implementation
def test_decorator_implementation_follows_p2_2a_design_requirements(self):
    """Verify decorator implementation matches P2.2a analysis requirements."""
    # Assert: Decorator signature matches design specification
    # Assert: Parameter injection follows analyzed patterns
    # Assert: Error handling matches design requirements

# Test P2.1b → P2.2b: Click integration to decorator usage
def test_decorator_uses_p2_1b_click_context_utilities(self):
    """Verify decorator uses Click context utilities from P2.1b."""
    # Assert: Decorator retrieves context using P2.1b utilities
    # Assert: Context storage/retrieval compatibility maintained
```

**DI Migration Context Testing (2+ tests):**
```python
# Test singleton compatibility during migration
def test_decorator_supports_singleton_migration_patterns(self):
    """Verify decorator supports migration from singleton to context injection."""
    # Setup: Function using singleton pattern
    # Action: Apply context injection decorator
    # Assert: Function behavior preserved with context injection

# Test migration compatibility with existing CLI structure
def test_decorator_maintains_cli_structure_during_migration(self):
    """Verify decorator doesn't break existing CLI command structure."""
    # Setup: Existing CLI command structure
    # Action: Apply context injection decorator
    # Assert: CLI structure and functionality preserved
```

**Quality Gate Validation:**
- [ ] All unit tests pass with 100% coverage for decorator implementation
- [ ] Integration test confirms complete context injection flow
- [ ] Manual tests validate Click compatibility and metadata preservation
- [ ] Cross-slice tests verify P2.2a design compliance and P2.1b utility usage
- [ ] DI migration tests confirm singleton compatibility and CLI structure preservation
- [ ] Type checking passes for all decorator utility functions
- [ ] Ruff linting passes with no decorator-related violations
- [ ] Pre-commit hooks validate decorator implementation quality
- [ ] Performance tests show no significant overhead from decorator application

**AI Agent Execution Notes:**
- Use decorator design requirements from P2.2a for implementation approach
- Integrate with Click context utilities from P2.1b for context retrieval
- Focus on clean decorator that preserves Click command functionality
- Ensure decorator maintains full compatibility with existing Click decorator chains
