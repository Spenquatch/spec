# Slice P3.1c: Gen Command Migration

**Goal**: Migrate gen command to use context injection decorator

**Slice Type**: Component

**Helper Dependencies & Search Evidence:**

Helper search performed and validated:
```bash
# Validate gen command current implementation
find /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli -name "*gen*" -name "*.py"
# Expected: gen.py command file location and structure

# Check gen command current signature and dependencies
rg "def.*gen|@click\.command.*gen" /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli --type py
# Expected: Current gen command implementation details

# Identify singleton usage in gen command
rg "settings|console|progress|singleton" /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli/commands/gen.py
# Expected: Current dependency access patterns in gen command
```

- **Existing helper**: P2.2b context injection decorator → Context injection decorator implementation
- **Existing helper**: P2.3b migration utilities → Command migration validation from previous command migrations
- **Existing helper**: spec_cli/utils/error_utils.py → Error handling for migration issues

**Complexity Analysis:**
- Decision points: 4/7 (decorator application, signature migration, dependency replacement, behavior validation)
- Helper calls: 2 (migration_utils for signature changes, error_utils for exceptions)
- McCabe validation: Pass - under 7 complexity with helper delegation

**Inputs → Action → Outputs:**
- **Inputs**: {gen_command_file: Path, existing_migration_patterns: Dict[str, Any]}
- **Action**:
  1. Apply context injection decorator to gen command using P2.2b implementation
  2. Update command signature to accept context parameter using migration_utils helper
  3. Replace singleton access with context attribute access
  4. Validate migrated command maintains identical behavior using migration_utils helper
- **Outputs**: {migrated_gen_command: Callable} or {migration_error: MigrationError}

**Files to Create/Modify:**
- `spec_cli/cli/commands/gen.py` (modify - apply decorator and use context)

**Integration Validation:**
Migrated gen command works identically to previous behavior while using context injection consistently with all other migrated commands

**Test Specifications:**

**Unit Tests:** (6+ tests in `tests/unit/cli/commands/test_gen_migration.py`)
- `test_gen_command_when_context_decorator_applied_then_receives_context_parameter`
- `test_gen_command_when_using_context_settings_then_accesses_settings_from_context`
- `test_gen_command_when_using_context_console_then_accesses_console_from_context`
- `test_gen_command_when_singleton_replaced_then_no_singleton_references_remain`
- `test_gen_command_when_migrated_signature_then_maintains_click_compatibility`
- `test_gen_command_when_behavior_validation_then_identical_to_original_behavior`
- `test_gen_command_when_context_injection_error_then_raises_migration_error`

**Integration Test:** (1 test in `tests/integration/test_gen_command_migration.py`)
- `test_gen_command_migration_when_full_workflow_then_consistent_with_all_migrated_commands`

**Manual Test:** (Shell script with validation)
```bash
#!/bin/bash
# Test migrated gen command behavior
cd test_area
spec init
echo "test content" > test.py
spec gen test.py
if [ $? -eq 0 ]; then
  [ -f .specs/test.py/index.md ] && [ -f .specs/test.py/history.md ]
  if [ $? -eq 0 ]; then
    echo "PASS: Gen command migration maintains functionality"
    exit 0
  else
    echo "FAIL: Gen command behavior changed"
    exit 1
  fi
else
  echo "FAIL: Gen command migration failed"
  exit 1
fi
```

**Cross-Slice Integration Testing:**
- `test_gen_command_when_used_with_migrated_add_then_context_consistent`
- `test_gen_command_when_used_with_migrated_commit_then_dependency_injection_works`

**DI Migration Context Testing:**
- `test_gen_command_when_context_factory_provides_dependencies_then_uses_injected_instances`
- `test_gen_command_when_singleton_elimination_complete_then_no_global_state_access`

**Quality Gate Validation:**
- [ ] Gen command uses context injection decorator from P2.2b
- [ ] All singleton references replaced with context attribute access
- [ ] Command behavior identical to pre-migration implementation
- [ ] Migration follows patterns from previous command migrations
- [ ] No remaining singleton import or usage patterns
- [ ] Context parameter properly typed and validated
- [ ] Error handling maintains existing patterns while using context

**AI Agent Execution Notes:**
- Follow exact migration patterns established in previous command migrations for consistency
- Apply context injection decorator from P2.2b to gen command function
- Replace singleton access patterns with context.settings, context.console, etc.
- Ensure gen command behavior is preserved exactly while changing internal implementation
