# Slice P2.3a: Command Structure Analysis

**Goal**: Analyze existing CLI command structure and singleton usage patterns

**Slice Type**: Integration Analysis

**Helper Dependencies & Search Evidence:**

Helper search performed and validated:
```bash
# Validate CLI command directory structure
find /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli -name "*.py" -type f
# Expected: All CLI-related files for structure analysis

# Analyze existing command implementations
rg "@click\.command|def.*command" /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli --type py
# Expected: Current Click command definitions for migration planning

# Check singleton usage in CLI commands
rg "singleton|Singleton|getInstance" /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli --type py
# Expected: Current singleton access patterns in CLI commands
```

- **Existing helper**: spec_cli/cli/ → Existing CLI command structure to analyze
- **Existing helper**: spec_cli/utils/error_utils.py → Error handling for analysis failures
- **New helper to create**: spec_cli/utils/command_analysis.py → `analyze_command_structure(cli_dir: Path) -> CommandStructureReport` and `identify_singleton_usage(command_file: Path) -> List[SingletonUsage]`

**Complexity Analysis:**
- Decision points: 4/7 (command discovery, structure analysis, singleton detection, error handling)
- Helper calls: 2 (command_analysis for structure inspection, error_utils for exceptions)
- McCabe validation: Pass - under 7 complexity with focused analysis responsibility

**Inputs → Action → Outputs:**
- **Inputs**: {cli_directory: Path, target_commands: List[str]}
- **Action**:
  1. Analyze CLI directory structure and command organization using command_analysis helper
  2. Document existing command implementations and Click patterns
  3. Identify singleton usage patterns in init and status commands
  4. Generate migration requirements for context injection implementation
- **Outputs**: {command_analysis: CommandStructureReport} or {analysis_error: CommandAnalysisError}

**Files to Create/Modify:**
- `spec_cli/utils/command_analysis.py` (new - CLI command structure analysis helpers)
- `phases/analysis/command_migration_requirements.md` (new - command migration specification)

**Integration Validation:**
Command analysis identifies actual CLI structure and singleton usage patterns for migration planning

**Test Specifications:**

**Unit Tests (6+ functions):**
```python
# tests/unit/utils/test_command_analysis.py
class TestCommandAnalysis:
    def test_analyze_command_structure_when_valid_cli_dir_then_returns_structure_report(self)
    def test_analyze_command_structure_when_empty_dir_then_returns_empty_report(self)
    def test_analyze_command_structure_when_invalid_dir_then_raises_analysis_error(self)
    def test_identify_singleton_usage_when_command_has_singletons_then_returns_usage_list(self)
    def test_identify_singleton_usage_when_no_singletons_then_returns_empty_list(self)
    def test_identify_singleton_usage_when_invalid_file_then_raises_file_error(self)
    def test_analyze_click_patterns_when_valid_commands_then_identifies_decorator_usage(self)

    @pytest.fixture
    def mock_cli_directory(self, tmp_path):
        cli_dir = tmp_path / "cli"
        cli_dir.mkdir()
        (cli_dir / "commands").mkdir()
        (cli_dir / "commands" / "init.py").write_text("""
@click.command()
def init():
    console = Console()
    return console.print("initialized")
""")
        return cli_dir

    @pytest.fixture
    def mock_command_file(self, tmp_path):
        cmd_file = tmp_path / "test_command.py"
        cmd_file.write_text("""
from spec_cli.config import Settings
def test_command():
    settings = Settings()
    return settings.get_value()
""")
        return cmd_file

# tests/unit/utils/test_command_analysis_helpers.py
class TestCommandAnalysisHelpers:
    def test_extract_decorator_patterns_when_click_decorators_then_returns_patterns(self)
    def test_extract_singleton_imports_when_singleton_usage_then_returns_import_list(self)
    def test_parse_command_signature_when_valid_function_then_returns_signature_info(self)
```

**Integration Test (1 end-to-end):**
```python
# tests/integration/utils/test_command_analysis_integration.py
class TestCommandAnalysisIntegration:
    def test_full_cli_analysis_with_real_command_structure(self, tmp_path):
        """Test complete CLI analysis flow with realistic command structure."""
        # Setup: Create realistic CLI structure with multiple commands
        # Action: Run full command analysis on CLI directory
        # Assert: Analysis correctly identifies all commands, patterns, and singleton usage
```

**Manual Test (shell script):**
```bash
# Test command analysis functionality
cd test_area
spec init

# Test 1: Analyze current CLI structure
python -c "
from spec_cli.utils.command_analysis import analyze_command_structure
from pathlib import Path
result = analyze_command_structure(Path('spec_cli/cli'))
print(f'✓ Found {len(result.commands)} commands')
print(f'✓ Identified {len(result.click_patterns)} Click patterns')
"

# Test 2: Identify singleton usage in init command
python -c "
from spec_cli.utils.command_analysis import identify_singleton_usage
from pathlib import Path
result = identify_singleton_usage(Path('spec_cli/cli/commands/init.py'))
print(f'✓ Found {len(result)} singleton usage patterns')
for usage in result:
    print(f'  - {usage.singleton_class}: {usage.usage_pattern}')
"

# Test 3: Validate analysis output structure
python -c "
from spec_cli.utils.command_analysis import analyze_command_structure
from pathlib import Path
result = analyze_command_structure(Path('spec_cli/cli'))
assert hasattr(result, 'commands')
assert hasattr(result, 'click_patterns')
assert hasattr(result, 'singleton_usage')
print('✓ Command analysis structure validated')
"
```

**Cross-Slice Integration Testing (2+ tests):**
```python
# Test P2.3a → P2.3b: Command analysis to init migration
def test_command_analysis_provides_init_migration_requirements(self):
    """Verify command analysis provides requirements for P2.3b init migration."""
    # Assert: Analysis identifies init command structure
    # Assert: Singleton usage patterns documented for migration
    # Assert: Migration requirements generated for init command

# Test P2.3a → P2.3c: Command analysis to status migration
def test_command_analysis_provides_status_migration_requirements(self):
    """Verify command analysis provides requirements for P2.3c status migration."""
    # Assert: Analysis identifies status command structure
    # Assert: Singleton usage patterns documented for migration
    # Assert: Migration requirements generated for status command
```

**DI Migration Context Testing (2+ tests):**
```python
# Test singleton detection accuracy for migration planning
def test_analysis_accurately_identifies_singleton_patterns_for_migration(self):
    """Verify analysis correctly identifies singleton patterns that need migration."""
    # Setup: Commands with various singleton usage patterns
    # Action: Run singleton analysis
    # Assert: All singleton usage patterns correctly identified and classified

# Test migration requirements generation for DI transition
def test_analysis_generates_comprehensive_migration_requirements(self):
    """Verify analysis generates complete requirements for DI migration."""
    # Setup: Complex CLI structure with multiple singleton patterns
    # Action: Generate migration requirements
    # Assert: Requirements cover all identified singleton usage patterns
    # Assert: Requirements provide specific migration guidance for each pattern
```

**Quality Gate Validation:**
- [ ] All unit tests pass with 100% coverage for command analysis utilities
- [ ] Integration test confirms complete CLI structure analysis
- [ ] Manual tests validate analysis accuracy on real CLI structure
- [ ] Cross-slice tests verify migration requirements generation for P2.3b and P2.3c
- [ ] DI migration tests confirm singleton pattern identification and migration guidance
- [ ] Type checking passes for all command analysis functions
- [ ] Ruff linting passes with no analysis-related violations
- [ ] Analysis output properly structured for downstream slice consumption
- [ ] Migration requirements documentation generated with actionable guidance

**AI Agent Execution Notes:**
- Focus on documenting actual CLI command structure and patterns in existing codebase
- Identify specific singleton usage in init.py and status.py commands
- Analyze CLI entry point structure for context setup requirements
- Create detailed migration requirements for targeted command updates
