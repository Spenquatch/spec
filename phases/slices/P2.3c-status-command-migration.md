# Slice P2.3c: Status Command Migration

**Goal**: Migrate status command to use context injection decorator

**Slice Type**: Integration

**Helper Dependencies & Search Evidence:**

Helper search performed and validated:
```bash
# Validate status command current implementation
find /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli -name "*status*" -name "*.py"
# Expected: status.py command file location

# Check status command current signature and dependencies
rg "def.*status|@click\.command.*status" /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli --type py
# Expected: Current status command implementation details

# Identify singleton usage in status command
rg "settings|console|progress" /Users/spensermcconnell/__Active_Code/spec-cli/spec_cli/cli/commands/status.py
# Expected: Current dependency access patterns in status command
```

- **Existing helper**: P2.3a command analysis → Command migration requirements and singleton usage patterns
- **Existing helper**: P2.2b context injection decorator → Context injection decorator implementation
- **Existing helper**: P2.3b migration utilities → Command migration validation from init command migration
- **Existing helper**: spec_cli/utils/error_utils.py → Error handling for migration issues

**Complexity Analysis:**
- Decision points: 4/7 (decorator application, signature migration, dependency replacement, behavior validation)
- Helper calls: 2 (migration_utils for signature changes, error_utils for exceptions)
- McCabe validation: Pass - under 7 complexity with helper delegation

**Inputs → Action → Outputs:**
- **Inputs**: {status_command_file: Path, migration_requirements: CommandStructureReport}
- **Action**:
  1. Apply context injection decorator to status command using P2.2b implementation
  2. Update command signature to accept context parameter using migration_utils helper
  3. Replace singleton access with context attribute access
  4. Validate migrated command maintains identical behavior using migration_utils helper
- **Outputs**: {migrated_status_command: Callable} or {migration_error: MigrationError}

**Files to Create/Modify:**
- `spec_cli/cli/commands/status.py` (modify - apply decorator and use context)

**Integration Validation:**
Migrated status command works identically to previous behavior while using context injection from decorators

**Test Specifications:**

**Unit Tests (6+ functions):**
```python
# tests/unit/cli/commands/test_status_migration.py
class TestStatusCommandMigration:
    def test_migrated_status_when_valid_context_then_executes_successfully(self)
    def test_migrated_status_when_missing_context_then_raises_context_error(self)
    def test_migrated_status_when_invalid_context_then_raises_validation_error(self)
    def test_migrated_status_preserves_original_behavior_with_context_injection(self)
    def test_migrated_status_maintains_click_command_attributes(self)
    def test_migrated_status_handles_git_errors_appropriately(self)
    def test_migrated_status_supports_existing_command_line_options(self)

    @pytest.fixture
    def mock_spec_context(self):
        return Mock(spec=SpecContext)

    @pytest.fixture
    def mock_git_operations(self):
        return Mock(spec=GitOperations)

    @pytest.fixture
    def original_status_behavior(self):
        # Capture original status command behavior for comparison
        return Mock()

# tests/unit/cli/commands/test_status_migration_reuse.py
class TestStatusMigrationReuse:
    def test_status_migration_reuses_p2_3b_migration_utilities(self)
    def test_status_migration_follows_init_migration_patterns(self)
    def test_status_migration_maintains_migration_utility_compatibility(self)
```

**Integration Test (1 end-to-end):**
```python
# tests/integration/cli/commands/test_status_command_integration.py
class TestStatusCommandIntegration:
    def test_status_command_full_migration_with_context_injection(self, tmp_path):
        """Test complete status command migration from CLI entry to execution."""
        # Setup: CLI application with context injection and migrated status command
        # Action: Execute status command through CLI interface
        # Assert: Command executes successfully with context injection
        # Assert: Behavior identical to pre-migration behavior
        # Assert: Git status information properly displayed
```

**Manual Test (shell script):**
```bash
# Test status command migration functionality
cd test_area
spec init

# Create some test content for status command
echo "test content" > .specs/test.md
spec add .specs/test.md

# Test 1: Verify migrated status command preserves original behavior
spec status
echo "✓ Status command displays expected output"

# Test 2: Verify context injection in status command
python -c "
from spec_cli.cli.commands.status import status
import inspect
sig = inspect.signature(status)
assert 'context' in sig.parameters
print('✓ Context parameter added to status signature')
"

# Test 3: Verify decorator application
python -c "
from spec_cli.cli.commands.status import status
assert hasattr(status, '__wrapped__')
assert hasattr(status, '__click_params__')
print('✓ Context injection decorator applied correctly')
"

# Test 4: Verify migration utility reuse
python -c "
from spec_cli.utils.migration_utils import migrate_command_signature
from spec_cli.cli.commands.status import status
# Verify migration utilities are properly reused
print('✓ Migration utilities reused from P2.3b implementation')
"
```

**Cross-Slice Integration Testing (2+ tests):**
```python
# Test P2.3a → P2.3c: Command analysis to status migration
def test_status_migration_follows_p2_3a_analysis_requirements(self):
    """Verify status migration follows requirements from P2.3a command analysis."""
    # Assert: Migration addresses singleton patterns identified in P2.3a
    # Assert: Click patterns maintained as documented in P2.3a
    # Assert: Migration follows analysis recommendations for status command

# Test P2.3b → P2.3c: Init migration to status migration consistency
def test_status_migration_follows_p2_3b_migration_patterns(self):
    """Verify status migration follows consistent patterns from P2.3b init migration."""
    # Assert: Status migration uses same migration utilities as P2.3b
    # Assert: Decorator application follows P2.3b patterns
    # Assert: Context injection approach consistent with P2.3b
```

**DI Migration Context Testing (2+ tests):**
```python
# Test singleton to context injection migration consistency
def test_status_migration_replaces_singleton_access_consistently_with_init(self):
    """Verify status migration replaces singleton access consistently with init migration."""
    # Setup: Original status command with singleton usage
    # Action: Apply migration to use context injection
    # Assert: All singleton access replaced with context attribute access
    # Assert: Migration pattern consistent with init command migration

# Test migration utility reuse and compatibility
def test_status_migration_reuses_migration_utilities_effectively(self):
    """Verify status migration effectively reuses migration utilities from P2.3b."""
    # Setup: Status command migration using P2.3b utilities
    # Action: Execute migration with shared utilities
    # Assert: Migration utilities work correctly for status command
    # Assert: No duplication of migration logic between commands
```

**Quality Gate Validation:**
- [ ] All unit tests pass with 100% coverage for migrated status command
- [ ] Integration test confirms complete status command migration flow
- [ ] Manual tests validate behavior preservation and context injection
- [ ] Cross-slice tests verify P2.3a analysis compliance and P2.3b pattern consistency
- [ ] DI migration tests confirm singleton replacement and utility reuse
- [ ] Type checking passes for migrated status command
- [ ] Ruff linting passes with no migration-related violations
- [ ] Migration utilities properly reused from P2.3b without duplication
- [ ] Status command maintains identical behavior while using context injection

**AI Agent Execution Notes:**
- Use migration requirements from P2.3a to guide specific status command changes
- Apply context injection decorator from P2.2b to status command function
- Reuse migration utilities from P2.3b for consistent migration approach
- Ensure status command behavior is preserved exactly while changing internal implementation
