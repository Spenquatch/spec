# Slice P3.2c: Pre-commit Hook Integration

**Goal**: Integrate singleton detection into pre-commit hooks for continuous validation

**Slice Type**: Component

**Helper Dependencies & Search Evidence:**

Helper search performed and validated:
```bash
# Validate current pre-commit configuration
find /Users/spensermcconnell/__Active_Code/spec-cli -name ".pre-commit-*" -o -name "pyproject.toml"
# Expected: Existing pre-commit configuration files

# Check pre-commit hook structure in pyproject.toml
rg "\[tool\.pre-commit\]|\[tool\.ruff\]" /Users/spensermcconnell/__Active_Code/spec-cli/pyproject.toml
# Expected: Current pre-commit tool configuration

# Validate hook script patterns
find /Users/spensermcconnell/__Active_Code/spec-cli -name "*hook*" -name "*.py" -o -name "*.sh"
# Expected: Existing hook scripts or none found
```

- **Existing helper**: P3.2b singleton detection system → Detection utilities and violation reporting
- **Existing helper**: spec_cli/utils/error_utils.py → Error handling for hook failures
- **New helper to create**: spec_cli/utils/hook_integration.py → `create_pre_commit_hook(detection_tool: Path) -> str` and `validate_hook_configuration(hook_config: Dict[str, Any]) -> bool`

**Complexity Analysis:**
- Decision points: 4/7 (hook creation, configuration validation, integration setup, error handling)
- Helper calls: 2 (hook_integration for setup, error_utils for exceptions)
- McCabe validation: Pass - under 7 complexity with helper delegation

**Inputs → Action → Outputs:**
- **Inputs**: {pre_commit_config: Path, detection_tool: Path}
- **Action**:
  1. Create pre-commit hook configuration using detection system from P3.2b
  2. Integrate hook with existing pre-commit setup using hook_integration helper
  3. Validate hook configuration and functionality
  4. Handle hook integration errors using error_utils helper
- **Outputs**: {hook_integration: HookIntegration} or {hook_error: HookIntegrationError}

**Files to Create/Modify:**
- `pyproject.toml` (modify - add pre-commit hook for singleton detection)

**Integration Validation:**
Pre-commit hooks prevent singleton pattern reintroduction through automated detection during development workflow

**Test Specifications:**

**Unit Tests:** (6+ tests in `tests/unit/utils/test_hook_integration.py`)
- `test_create_pre_commit_hook_when_detection_tool_provided_then_creates_valid_hook`
- `test_validate_hook_configuration_when_valid_config_then_returns_true`
- `test_validate_hook_configuration_when_invalid_config_then_returns_false`
- `test_hook_integration_when_singleton_detected_then_prevents_commit`
- `test_hook_integration_when_clean_code_then_allows_commit`
- `test_pre_commit_hook_when_configuration_error_then_raises_hook_integration_error`
- `test_hook_execution_when_detection_system_unavailable_then_handles_gracefully`

**Integration Test:** (1 test in `tests/integration/test_pre_commit_hook_integration.py`)
- `test_pre_commit_hook_integration_when_full_workflow_then_prevents_singleton_reintroduction`

**Manual Test:** (Shell script with validation)
```bash
#!/bin/bash
# Test pre-commit hook integration
git init test_hook_repo
cd test_hook_repo
cp ../pyproject.toml .
echo "class TestSingleton(metaclass=SingletonMeta): pass" > bad_code.py
git add bad_code.py
pre-commit run --all-files
HOOK_RESULT=$?
cd ..
rm -rf test_hook_repo

if [ $HOOK_RESULT -ne 0 ]; then
  echo "PASS: Pre-commit hook prevents singleton code"
  exit 0
else
  echo "FAIL: Pre-commit hook failed to detect singleton"
  exit 1
fi
```

**Cross-Slice Integration Testing:**
- `test_hook_integration_when_detection_system_installed_then_uses_singleton_detector`
- `test_pre_commit_hook_when_infrastructure_removed_then_validates_clean_commits`

**DI Migration Context Testing:**
- `test_hook_integration_when_migration_complete_then_maintains_singleton_free_development`
- `test_pre_commit_validation_when_context_injection_used_then_allows_proper_di_patterns`

**Quality Gate Validation:**
- [ ] Pre-commit hook configuration integrates cleanly with existing setup
- [ ] Hook uses singleton detection system from P3.2b for validation
- [ ] Hook prevents commits containing singleton patterns
- [ ] Hook provides clear feedback about singleton violations
- [ ] Hook configuration is maintainable and documented
- [ ] Hook integration doesn't disrupt existing development workflow
- [ ] Hook handles detection system errors gracefully without blocking all commits

**AI Agent Execution Notes:**
- Use singleton detection system from P3.2b for hook implementation
- Integrate cleanly with existing pre-commit hook configuration
- Ensure hook provides clear feedback to developers about singleton violations
- Focus on seamless integration that doesn't disrupt existing development workflow
